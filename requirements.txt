# Echolog - Audio Recording Application Dependencies

# Core dependencies
psutil>=5.9.0          # Process management and system monitoring
configparser>=5.3.0    # Configuration file handling
# argparse is built-in with Python, no need to install

# Optional GUI dependencies
# tkinter is built-in with Python, no need to install

# Audio processing (if needed for post-processing)
pydub>=0.25.1          # Audio file manipulation
mutagen>=1.45.0        # Audio metadata handling

# Development dependencies
pytest>=7.0.0          # Testing framework
pytest-cov>=4.0.0      # Coverage reporting
